# src/

## Responsibility
Root source directory containing all backend application modules, domain business logic, API endpoints, CLI tools, and adapters.

## Sub-Packages
- `app/`: Main application runtime, API routes, CLI, and domain sub-packages.
